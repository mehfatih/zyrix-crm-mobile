EXPO_PUBLIC_API_URL=https://api.crm.zyrix.co
EXPO_PUBLIC_API_VERSION=v1
EXPO_PUBLIC_GOOGLE_MAPS_KEY=
EXPO_PUBLIC_SENTRY_DSN=
# Serve built-in demo/mock data instead of the real backend. OFF by default.
# Set to "true" ONLY for local offline demos — never in a release build.
EXPO_PUBLIC_USE_MOCKS=false
